{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm --prefix backend run migrate && npm --prefix backend start",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 100
  }
}
