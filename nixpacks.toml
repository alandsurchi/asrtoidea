# Root fallback for Railway: if a service is accidentally pointed at repo root,
# build and run the backend from backend/ instead of attempting a Flutter build.

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm --prefix backend ci"]

[start]
cmd = "npm --prefix backend run migrate && npm --prefix backend start"
